version: "1"

services:
  db:
    image: mongodb
    env_file:
      - .env
  app:
    build:
      dockerfile: Dockerfile
      context: .
    image: riccardompesce/dsbd-mmms
    env_file:
      - .env
    depends_on:
      - db
    ports:
      - "8080:8080"