version: "1"

services:
  db:
    image: mongo
    env_file:
      - .env
  app:
    build:
      dockerfile: Dockerfile
      context: .
    volumes:
      - ./app:/code/app
    image: riccardompesce/dsbd-mmms
    env_file:
      - .env
    depends_on:
      - db
    ports:
      - "8080:8080"